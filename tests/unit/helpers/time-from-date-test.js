import { timeFromDate } from '../../../helpers/time-from-date';
import { module, test } from 'qunit';

module('Unit | Helper | time from date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timeFromDate(42);
  assert.ok(result);
});
